import React from 'react';

export const metadata = {
  title: 'Login - TechMind',
  description: 'Página de autenticação do sistema TechMind',
};

export default function LoginLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}